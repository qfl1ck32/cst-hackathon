import { Schema, Is, a, an } from "@bluelibs/validator-bundle";
import { EndUserBookUpdateInput as BaseEndUserBookUpdateInput } from "./EndUserBookUpdate.input.base";

@Schema()
export class EndUserBookUpdateInput extends BaseEndUserBookUpdateInput {
  // You can extend the base here
}
